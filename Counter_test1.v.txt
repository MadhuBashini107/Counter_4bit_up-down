`timescale 1ns/1ps
module counter_tb;
reg clk,rst,m;
wire [3:0] count;
counter uut (clk,rst,m,count);
always #5 clk = ~clk;
initial
begin
	clk = 0;
	rst = 0; #5;
	rst = 1;
	m = 1; #160;
	m = 0;
	#160;
	$finish;
end
endmodule
